/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.wildnatureimages.com'],  // Allow loading images from unsplash.com
      },
};

export default nextConfig;
